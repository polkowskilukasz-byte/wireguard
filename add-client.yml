---
- hosts: localhost
  gather_facts: false

  tasks:
    - name: Test variables
      debug:
        msg:
          - "CLIENT={{ client_name }}"
          - "IP={{ client_ip }}"
          - "LAN={{ client_lan }}"
          - "AWX działa"
